/**
 * This class runs the Uber App Program
 */
public class UberApp {

    public static void main (String[] args) throws Exception{
        // loads intersections 
        RouteLoader loader = new RouteLoader();

        // adds each intersection to the backend object

        // creates a frontend with backend object
        UberPickupFrontend
    }
    
    
}
